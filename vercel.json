{
    "version": 2,
    "builds": [
      {
        "src": "nuxt.config.js",
        "use": "@nuxtjs/vercel-builder",
        "config": {"runtime":"python3.12.0"}
        }
    ],
    "routes": [
        {
            "src":"/(.*)",
            "dest":"/"
        }

    ]
}